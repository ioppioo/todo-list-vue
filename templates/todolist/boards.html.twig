{% extends "base.html.twig" %}

{% block body %}

    <div class="boards">

        <div class="boards-board-new">
            <div class="board-new-title">
                <a href="/logout"
                   class="board-title-text">Выход</a>
            </div>
        </div>

        <div class="boards-board-new">
            <div class="board-new-title">
                <button class="board-title-text">Новая доска</button>
            </div>
        </div>

        {% for board in boards %}

            <div class="board" data-board-id="{{ board.id }}">
                <button class="button button-task-del js-board-remove">🞫</button>
                <div class="board-title">
                    <a href="/boards/{{ board.id }}"> <span class="board-title-text">{{ board.title }}</span></a>
                    <a href="/boards/{{ board.id }}/edit" class="button button-edit">✎</a>
                </div>
            </div>

        {% endfor %}
    </div>

{% endblock %}